
[phases.setup]
nixPkgs = [
    "poppler_utils",
    "libreoffice",
    "qpdf",
    "python3"
]

[phases.build]
cmds = [
    "npm install",
    "python3 -m venv venv",
    "./venv/bin/pip install --no-cache-dir pdf2docx PyMuPDF python-pptx"
]

[start]
cmd = "npm start"
